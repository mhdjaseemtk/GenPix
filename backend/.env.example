# Optional: add a Pollinations API key if your account or deployment needs one
POLLINATIONS_API_KEY=

# Optional: override the upstream image host if Pollinations changes endpoints
POLLINATIONS_BASE_URL=https://image.pollinations.ai
